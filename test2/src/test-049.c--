// execute the commands below to use this file as a test case
//

target byteorder little;

g () 
{
    import bits32 f;
    
    bits32 x,y,z;
    bits16 a,b,c;
    bits1  n;
    
    bits1[a] = n;
    bits1[x] = n;
    bits32[a] = x;
    "foo" x = f () targets f;
    "foo" x = f (); 
    "bar" x = %eq(x,y);
}

