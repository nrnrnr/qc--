target byteorder little;

import bits32 printf;
export main;

foreign "C" main("unsigned" bits32 iargc, "address" bits32 iargv) {
  bits32 dead;

  cut to k("signed" 97) also cuts to k;

  continuation k(dead):
  foreign "C" return(0);
}
