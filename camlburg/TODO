vim: set ts=8 et sw=4:

    $Id$

- make code more imperative; replace { foo with x = ..} with (foo.x <- ..; foo )
- sort calls for chain rules according to cost
- bind subject tree variables for cost expressions
- write man page
