vim: set ts=8 et sw=4:

    $Id$

 o  Make code more imperative; replace { foo with x = ..} 
    with (foo.x <- ..; foo )
 o  Sort calls for chain rules according to cost
 o  Introduce %tail {: :} declaration? 
 o  spec ::= decls "%%" rules "%%" decls ?
