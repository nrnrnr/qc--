
This directory contains tools to test calling conventions. The idea is
to generate calls that pass parameters whose value are checked by the
callee. Calls are generated from a list of "interesting" signatures
which in turn are derived from an automaton that implements a calling
convention.

