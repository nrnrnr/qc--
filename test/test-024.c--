const     true = 1 == 1;
// execute the commands below to use this file as a test case
//
/// ./fp test-024.c--
/// ./regr "../bin/qc-- -e 'Main.check()' $SELF" $REGRFLAGS  -err $BASE.2 -x 1

target byteorder little;


foo() {
    bits1 x;

    span 1 true {
        if (true) {
            ;
        }
    }

    return;
}
