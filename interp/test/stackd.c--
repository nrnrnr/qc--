// stackd.c--

target byteorder little;

export main;
import bits32 printf;

section "data" {
  print: bits8[] {'%'::bits8, 'd'::bits8, '\n'::bits8, 0::bits8};
}

section "text" {
  main() {
    bits8 x;
    bits32 addr;

    stackdata {
      slabel:  bits8;
      slabel2: bits8;
    }

    addr = slabel;
    addr = slabel2;

    return;
  }
}