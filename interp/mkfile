TOP=..
INTERP=evaluating

<../config/noweb.mk

all:V: design.ps

design.tex: design.inc
	echo "\input{design.inc}" > $target

state.tex: state.inc
    echo "\input{state.inc}" > $target

clean:V: clean-tex
	rm -f design.tex state.tex *~

clean-tex:V:
	rm -f *.blg *.log *.dvi *.inc *.toc *.aux
