<<implementation>>=
 (*type test = Null | Int of int | Pair of { x : int; y : int }*)

type color = Yellow | Green
type banana = {color: color; how_many: int}
let fish = 3

type printer = {color: bool; name: string}

let sum a b : int = a + b

