target byteorder little;

section "text" {
        p () {
                bits32 i, j, k, ntotal;
                i = 1;
	        j = 2;
        	k = 3;

                ntotal = i + j + k;
		return (ntotal);
        }
}
