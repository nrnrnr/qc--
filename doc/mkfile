#
#

TOP     = ..

all:V:          cfg.ps better.ps working.ps newtarget.ps

<../config/noweb.mk

cfg.tex:        cfg.nw
                $NOWEAVE -delay cfg.nw > $target 

clean:V:
                rm -f *.dvi *.aux *.log *.toc                
                rm -f cfg.tex
